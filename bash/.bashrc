# .bashrc

# User specific aliases and functions
alias ll="ls -la"
alias gs="git status"
alias gp="git pull"
alias gc="git commit"

# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

